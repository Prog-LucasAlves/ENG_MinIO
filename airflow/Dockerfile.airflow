FROM apache/airflow:2.9.1

# Instalar dependências adicionais se quiser (exemplo)
# RUN pip install boto3 flask

COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
